# 飞书上方复刻流程接入与验证记录

更新：2026-09-18。用户已提交并上线 `314ff22`。该版本的本地模拟 HTTP 验证、生产构建、TypeScript 与相关 ESLint 检查通过。下述真实音轨验证发现了新的转录问题；修复正在独立候选镜像验证，本轮修复尚未提交或部署。

已接入两条原文完整的基础流程。提示词来源限于用户指定飞书多维表格上方工作流；归档正文保持不变，运行时按原片时长分配组、调整对应时长与镜头数量参数，并绑定字段数据。

| 运行流程 | 飞书来源 | 原文决定的行为 |
| --- | --- | --- |
| `product-basic` | `tbl7CUSql4kncNvk`，换品_不换人 1 分钟基础版 | 单步生图，参考顺序为产品图 → 本组原分镜拼图。虽然表名写“不换人”，原文会重绘出现的人物，产品替换仅发生于人物或手部持有产品的镜头；不能指定人物或背景。 |
| `person-basic` | `tblWsGmy3C9igdZ2`，不换品_换人 1 分钟基础版 | 保留原产品，单步生图，参考顺序为本组 12 张独立原帧 → 必填背景图 → 可选人物图。 |

2026-09-19 输入适配修正：此前跟品生图误传本组拼图，与归档原文要求的“12张分镜图片”不一致。已改为传入完整独立原帧，附件字段说明与实际图片共用同一顺序；原提示词正文未改。同时将分镜任务从 `generation` 改为 `edit`，避免 OpenAI 图片协议按文生图发送、不携带参考图；任务入口对已通过分组校验的请求也统一按图生图处理。旧生成结果保留，并提示重新生图。按用户要求，本次未运行测试、构建或真实生成，未提交或部署，不能据此宣称生成效果已通过验收。

2026-09-19 来源与输入说明：用户反馈人物消失、分镜错位。只读查看现有生产项目 `frame-remake-b5546230-64b6-4f63-8488-1a68150c715b` 的素材页面，人物槽为空、背景已绑定。按飞书原文，人物图可选、背景图必填；人物槽为空是合法输入，不能据此解释人物消失，更不能擅自改成人物图必填。页面将该流程明确标为“保留产品·换背景（可指定人物）”，说明原文的人脸模糊规则，并提供对应表的「1-12 生图」来源链接。换品基础流程不要求背景图。分镜提示词取自本地飞书归档，参考图生成弹窗的默认描述和 AI 优化指令属于系统补写，已单独标明来源。多图 OpenAI JSON 请求重试中仅发送首图的降级分支已移除，避免漏掉其他分镜及人物素材；未确认本次生产请求实际走过该分支。本轮未进行模型生成验收，不能据此认定人物消失或错位问题已解决。未测试、未提交、未部署。

2026-09-19 背景未替换的只读诊断：同一项目三组已完成任务的 `referenceUrls` 都有 13 张图，前 12 张为各组原帧，第 13 张都等于当前 `references.background[0].url`；各组保存的提示词也明确把图片 13 标为背景图。该背景实际为米色墙面、镜柜与白色洗手台。截图对应第二组任务 `633a5ffb-5364-47e6-903c-1faef6248795`，管理记录显示模型 `banana-pro`、协议 `custom`，因此不能把 OpenAI JSON 首图降级当作该任务的已确认原因。当前渠道模板将完整图片数组放在 `params.images`，但参考说明写 `image_urls`，两者不一致；服务商文档目前要求登录，尚未确认实际接口契约，未擅自修改渠道配置。现有记录不包含上游最终接收的完整请求，不能据此区分渠道漏读与模型未遵循指令。项目的 `completed` 仅表示已取得并保存图片，不包含背景替换的视觉验收。本次没有重新生图、测试、提交或部署。

两条流程独立保存来源分析、拆帧、原文案转录、生图、文案预处理、视频提示词、视频与合成结果。文案预处理提供原表的 A 保持原文、B 自定义优化选项。`combined-original` 的产品脚本和两步生图模块正文仍不齐，执行被阻止，已有结果可以查看；没有用其他表的提示词补写缺失模块。

视频提示词按第 1–4 组选择 `1–12 / 13–24 / 25–36 / 37–48` 原文，第 5 组起沿用已核对的通用后续模板。换品流程的后续视频模板明确共用 `tblWsGmy3C9igdZ2` 表的对应字段，执行记录与预览保留真实来源链接，不标成换品表原文。仅机械映射本组编号与时长，6 项临时回归通过并已删除。运行时 17 条来源正文与独立档案逐条一致，其中 11 条有完整正文。

来源记录见 `docs/frame-remake-upper-workflow-discovery.source.json`；完整正文见 `docs/frame-remake-upper-workflow-prompts.source.json`。表中已记录的原始引用冲突继续保留在归档中。

已完成验证：

- 服务与运行流程集成 22 项、转录回归 21 项、取消状态回归 8 项全部通过，相关临时测试文件已删除。外部模型、转录及存储接口由测试替身提供，不能视为真实服务商生成成功。
- 使用真实 FFmpeg 验证 15、20、120 秒时间线与合成，测量时长误差均为 0 ms。短尾段将完整生成片段对齐原片时长，保留末尾镜头。
- 浏览器实际检查了末尾 24 帧、右侧详情滚动以及暂停、取消操作。取消最后活跃任务后遗留顶部错误和“仍在处理”进度的问题已进一步修复，并通过上述 8 项回归；其他任务的真实提醒保留。
- 本机真实 Next 服务、JSON 数据库、Worker 和模拟上游接口完成 HTTP 验收：G1 单步生图成功保存并自动暂停，G2 保持 `idle`、`attemptNo: 0`；随后 G2 提交至 `running` 时通过 DELETE 取消，任务进入已取消终态，项目 `error` 和 `operation` 清空，G1 图片结果保留。6 秒后再次检查，状态未反弹。上游为模拟接口，此项验证的是实际应用链路，不能作为真实模型生成成功的证据。

飞书“原文案”字段是阿里百炼的“音频视频转文字”快捷功能，没有创作提示词正文。生产环境当前没有百炼 ASR Key。转录服务复用旧 `remake15-analysis-runtime.ts` 的 `sourceCopy` 说明和 JSON 协议：“按视频原语言逐字转写的完整口播，不得翻译；无口播时返回空字符串”。这属于服务适配协议，不是飞书创作提示词；本轮没有修改归档创作正文。

真实转录验证：独立 6 秒视频画面写 `5529`，实际音轨读“The spoken code is seven three nine one.”。生产 `314ff22` 的 Doubao Pro 转录在 12,274 ms 后返回空字符串，错误保存为 `no-speech`。操作 POST 在 112 ms 返回，失败原因是将视频画面理解能力当成了音轨理解能力，并非浏览器 180 秒超时。

同一真实音轨提取为 16 kHz 单声道 WAV，通过现有系统代理、现有 `gem-3-pro` 渠道，以原生 Chat `input_audio` 调用，HTTP 200，9,722 ms 返回 `{"sourceCopy":"The spoken code is 7391"}`。输入只有音频，不存在读取画面数字的可能。未新增模型凭据。

候选镜像 `vozeb-pro:frame-audio-20260918` 完成真实转录操作集成验证：同一独立诊断项目经 `/operations` 在 170 ms 返回 HTTP 202，10,530 ms 后保存 `transcribed`、`The spoken code is 7391.`、真实模型 `gem-3-pro` 与协议 `openai-chat-input-audio`，无错误。前后核对原“牙膏”项目 JSON 哈希一致。候选容器只监听服务器 127.0.0.1:3221，尚未替换正式服务；Docker 构建内 TypeScript 和生产构建通过。

修复改为独立选择支持音轨的已配置模型，画面分析仍用原模型。Gemini 白名单仅含已实测的 `gem-3-pro`；Doubao 音频型号依据官方音频能力列表识别 Lite/Mini 260428，Pro 260215 不参与转录。记录真实模型与传输协议。旧记录缺元数据、协议不匹配或使用不支持音轨的模型时，页面要求重新转录或人工核对；文案、视频提示词和视频提交入口均阻止继续使用该记录。所有自动转录来源（含百炼）还必须有完成时间且没有错误，防止重转失败或暂停后复用旧状态误判就绪；此项追加修改已在本地验证，并进入 `vozeb-pro:frame-audio-final-20260918` 独立候选镜像，构建与健康检查通过。

本轮修复验证：19 项音频请求与真实 FFmpeg/WAV 回归、12 项前端与就绪状态回归、15 项服务门禁回归以及追加的 9 项转录完成状态回归通过。覆盖错误退款头、取消、拒绝未验证音频协议、旧记录校验、人工确认，以及排队任务和独立任务 API 的门禁。临时测试均已删除，已有测试保留；最终本地生产构建（含 TypeScript）、相关 ESLint 和 diff 检查通过。

上轮本机模拟环境及端口 3219/3220 已清理。当前远程诊断项目仅用于验证，与原“牙膏”项目分离。原项目没有产品参考图，也未选择生图或视频模型，验收副本单独使用标注 TEST ONLY 的测试牙膏图，以及已有默认 `banana-pro`、`doubao-seedance-2-0-260128`，未修改用户原项目。验收副本 `frame-remake-393b3f6f-f9bf-49de-a978-773c4fc64c23` 已修正 JSONB 字段顺序导致的校验误报，采用递归排序后的内容哈希核对。第一组真实生图已通过：`banana-pro` 返回并保存 1536×2720 PNG，任务状态为 `completed`，原项目哈希保持一致。第一组文案预处理已返回完整 4 个字幕区间，视频提示词已生成（1388 字）。真实视频提交被上游以输入图可能涉及版权限制拒绝（HTTP 502），已停止使用该素材验证。再次 GET 后任务正确收敛为 `error`，没有重复提交或更换方式绕过拒绝。按该测试请求时间、模型及所有者核对消费流水，消费 1 点已全额退回。

另建完全自制的 20 秒测试视频（无人物、蓝绿色管状产品、MINT / TEST ONLY），验收项目 `frame-remake-40ca4fe4-8917-4af3-98cf-66fbf557de3b`。实际读取为 20,000 ms，自动划分为 15 秒及 5 秒两组。G1 来源分析、12 帧提取和无音轨判断成功，G2 分析未通过连续时间码校验；此时尚未提交该自制素材的图片或视频生成请求。

失败诊断补丁保存未通过解析的模型原文和实际出错区间，页面可查看；不放宽时间校验。新增 3 项临时回归通过，验证失败原文保留、未完成状态、退款，以及取消后迟到结果不覆盖项目；测试文件已删除，TypeScript 与相关 ESLint 通过（2 项原有 warning）。诊断镜像构建通过，并对 G2 做了单次带诊断重试。实际返回完整连续的分镜 1–12，最后区间为 4.5–4.9 秒，文件实际为 5 秒；失败原文、提示词与具体区间已保存。

针对此实测尾差，解析仅允许把最后一镜头不超过 100 ms、且小于该镜头自身长度的缺口补齐至真实结束时间。中间缺口、重叠、任何越界和更大尾差仍拒绝；保留原分析正文，保存对齐记录并在页面说明。8 项针对性回归通过，临时测试已删除；TypeScript、相关 ESLint 和 diff 检查通过。飞书创作提示词及机械参数渲染未修改。候选源码 10 个文件与本地逐字节 SHA256 一致：`c416ca0732e1f418adf1026be302de550066a7a4141961c4028c10494b0e8db6`，修复镜像正在构建。真实生成全流程尚未通过，不能据此宣称全部功能已验收。本轮远程临时脚本与测试素材仍待完成验证后清理。仍在 DEV 分支；本轮没有暂存、提交或推送。
