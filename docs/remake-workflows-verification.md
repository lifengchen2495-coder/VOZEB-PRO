已完成当前飞书 Base 去重后的 5 条新增复刻流程，各自使用独立功能入口、API 和项目存储；原 /remake 与 /remake15 保留。

菜单已按后续要求统一为「电商创作」（/commerce），桌面侧边栏和手机导航均只保留一个总入口。总入口内有 7 种视频复刻和「电商图生成」，复刻列表页可通过页头返回总入口，项目地址与数据保持原有归属。

「电商图生成」位于 /commerce/image，复用现有 Agent 图片工作区，默认选择图片生成模式和电商生图能力，支持商品主图、场景图、详情页和营销海报。新建及历史对话链接留在电商图入口；/create 继续使用原始 Agent 默认状态。菜单和 Agent 相关回归、TypeScript 与 ESLint 检查通过；浏览器验收记录位于 .workflow/commerce-menu-browser-review.json，生成请求在浏览器拦截验证参数，没有调用付费模型。

| 菜单 | 来源流程 | 完整路径 |
| --- | --- | --- |
| /remake60 | 1 分钟换品换人 | 48 帧、适配新产品脚本、两步生图、4 条 15 秒视频、合并与导出 |
| /remake-product | 1 分钟换品不换人 | 48 帧、单步换品、锁定原人物和背景、4 段视频、合并与导出 |
| /remake-person | 1 分钟换人不换品 | 48 帧、背景必需、人物与补充可选、原产品不变、4 段视频、合并与导出 |
| /omni-clothing | Omni 服装 | 来源视频自然或手动切片、4–5 张服装图、逐片生成、原任务恢复、音频合并与完整 ZIP |
| /omni-remake | Omni 全品类 | 全片分析、口型音频策略、素材与双语计划、逐片生成、原任务恢复、按原时长合并与导出 |

来源核对记录见 remake-workflows.source.json 和各模块 *-feishu*.source.json。当前共核对 12 张表，重复实操/测试/空白表按同一流程处理，素材库与分析明细不另建复刻菜单。没有发现独立 30 秒表。

验证日期：2026-09-08。

- 72 个测试文件、697 个不同测试最终全部通过，无跳过。首轮 687 项有 1 项数据库表数断言未同步（62→67）；修正后该文件 7 项复跑通过，另外补充 10 项新流程恢复测试通过。
- TypeScript 全局检查通过；新增模块及共享改动 ESLint 通过。
- 真实本地 FFmpeg 覆盖四段 60 秒合并、横竖画幅、有声/无声混合，以及 Omni 按小数时间切片、原声恢复、完整成片和失败清理。
- Edge + 独立 Next dev 实际验证 5 个新菜单、新建、手动/自动保存、缺视频拦截和项目存储隔离；使用隔离测试账号、文件数据及空模型渠道。服装 InputNumber 废弃属性警告已修复，相关 4 项测试复跑通过；全品类主按钮颜色已按实际浏览器样式修复。
- 修复了旧 15 秒脚本图片数量约束与真实单图/双图输入不匹配问题，4 项组合测试通过。

本次没有真实调用付费模型，没有修改飞书表格，没有部署，也没有执行 git add、commit、push 或 tag。PostgreSQL 使用 schema/查询合同测试，未连接线上数据库验证。Next dev 已停止，自动修改的 next-env.d.ts 和 tsconfig.json 已恢复；测试记录留在 .workflow/remake-workflows-*。

后台需要配置支持完整视频理解的 Doubao 文本渠道、对应视觉文本及视频参考模型。Omni 单源文件最多 200 MB，全品类单段最多 10 秒且最多 200 段；不支持所需引用或时长的供应商会在提交前被拒绝。
