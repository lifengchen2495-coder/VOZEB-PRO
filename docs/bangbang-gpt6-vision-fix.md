# GPT-6 XHigh 产品原创看图兼容修复

2026-09-10，用户已上传产品图片，进入“创作方向”后提示“当前提示词模型没有可用的图片理解能力”。该提示来自 `web/src/lib/server/bangbang-runtime.ts` 的 `resolveCandidate`，发生在上游模型调用之前。

此前保存的 GPT-6 配置使用模型级 `custom` 协议、`/v1/chat/completions` 路径及 `reasoning_effort: xhigh`。产品原创调用的 `resolveRemakeProductionVisionProtocol` 原来会排除所有自定义协议，即使打开“参考图片”也不能通过。本地测试已复现此问题。

本次修复位于 `web/src/lib/server/remake15-production-vision-runtime.ts`：允许使用标准 Chat 路径、标准正文结果字段、完整 `{{model}}` 和 `{{messages}}` 占位符的自定义模板；将实际图片作为 `image_url` 内容项传入，并保留 XHigh 等模板参数。仍需显式启用图片能力；不接入会丢失图片的纯文本模板，不接受截断或拒绝结果。现有图片数量与大小限制继续生效。

## 线上生效步骤

1. 部署上述运行时代码，更新运行带货短剧任务的 Web／Worker 实例。
2. 后台 → 模型渠道 → 欢聚AI GPT 文本 → 渠道配置 → 高级设置 → 模型级路由，选择 `gpt-6`。
3. 展开“请求模板与参考素材”，勾选“参考图片”。保留模型级 `custom` 协议、`/v1/chat/completions` 路径及 `choices[0].message.content` 结果字段。
4. 请求模板保留如下结构。如果其他任务已将 `stream` 改为 `{{stream}}`，不必改回，本次视觉调用也能处理该占位符。

```json
{
  "model": "{{model}}",
  "messages": "{{messages}}",
  "reasoning_effort": "xhigh",
  "stream": false
}
```

5. 检查“逻辑模型”中 `gpt-6` 的渠道绑定：如果该绑定显式关闭了“参考图片”，也需启用，因为绑定配置优先于模型配置。保存模型渠道配置。
6. 回到“清洁剂”项目，重新生成创作方向，核对结果已保存及中转实际模型。此次错误发生在生成前，不需要重新上传已保存的产品图。

## 验证与限制

定向回归覆盖 GPT-6 XHigh 的多模态请求、布尔与占位符 stream、未声明图片能力的拒绝、错误模板拒绝、图片数量限制、上游错误及计费头保留、截断／拒绝结果和原生 Chat 请求。请求使用模拟响应，没有调用付费模型。

实际验证：3 个测试文件共 40 项通过；全项目 TypeScript 检查、两个变更 TypeScript 文件的 ESLint 和 `git diff --check` 通过。

当前浏览器连接报 `unsupported Codex auth method: apikey`，无法读取或保存线上后台配置。本次未部署、未修改线上渠道、未重新提交该项目的付费任务。修复不能据此视为线上验收通过。未执行 Git 暂存、提交或推送。

## 2026-09-11：创作方向的 HTTP 504 修复

用户最新截图明确显示“生成渠道网关等待响应超时（HTTP 504）”。代码确认棒棒有图步骤在视觉运行时强制 `stream: false`，无图步骤也使用同步结构化请求。即使管理员将模型模板改为 `{{stream}}`，旧视觉流程仍会传入 `false`。

棒棒现在对使用受信任标准 Chat 协议的 GPT-6 系列模型发起 `stream: true`，包括产品原创方向、剧本和后续无参考图的步骤。视觉运行时只在调用方明确请求流式时覆盖模板的 `stream`，仍保留 `gpt-6`、`reasoning_effort: xhigh` 和完整图片消息；既有 `stream: false` 或 `{{stream}}` 模板均可生效。无图步骤继续请求 JSON 对象。此次修复不需要先运行历史 SQL，也不修改其他业务的模型设置。其他模型及 Responses、Gemini 等协议继续沿用原请求流程。

系统代理对 SSE 响应设置 `X-Accel-Buffering: no` 和 `Cache-Control: no-cache, no-transform`，持续透传心跳与模型事件。视觉读取保留 UTF-8 和片段空格，只采纳第一个候选的正文，忽略推理、心跳和用量事件；收到 `finish_reason: stop` 与 `[DONE]` 才交付，截断或拒绝不会保存为成功。处理完流尾计费事件后，成功和失败均带回实际结算头，供棒棒按对应记录处理积分。单次响应最多读取 8 MiB，不做失败后的同步重试。

本地 HTTP 集成验证使用同一网关 fixture：同步请求返回 HTML 504，流式请求发送心跳、分块结果、完成标记和计费尾帧。隔离加载 HEAD 旧代码时，方向和无图分镜步骤均重现 504；当前代码下，完整方向、无图九帧、截断退款及非 GPT-6 原请求路径 5 项回归通过。最终定向测试共 4 个文件、44 项全部通过；全项目 TypeScript、变更 TypeScript 文件的 ESLint、生产构建与差异空白检查均通过。该验证没有调用真实 GPT，不能证明线上网关一定已正确转发心跳。

上线需要发布包含 `vision-chat-stream.ts`、视觉运行时、`bangbang-runtime.ts` 和系统代理路由的同一代码版本，并重建运行任务的 Web 服务。仅刷新网页、改超时提示或拉取旧 `v0.0.7` 镜像不会带入该修复。原产品图片无需重传。部署后在原“清洁剂”项目读取最新版本并生成方向，必须确认方向结果已保存，再验证下一步剧本；检查中转记录实际请求模型及流式状态。

目前仍未取得可用的线上连接，未部署该流式修复、未改线上模型配置，也未创建 Git 提交。若上游收到 `stream: true` 后仍因思考阶段没有任何心跳而返回 504，还需在发生超时的上游网关调整读取超时或启用心跳；本地增加超时时间无法覆盖远端网关的限制。

## 2026-09-11：文本请求超时配置生效

另一张截图显示“生成接口响应超时”，项目查询成功，但 `directions` 和 `outputs` 为空。代码中 `model-request-policy.ts` 对所有文本请求直接返回 180 秒，导致后台逻辑模型绑定的 `timeoutMs` 被忽略。该限制也作用于流式读取全过程；心跳不能延长总时限。它是本地确认的缺陷，截图本身不能证明线上这次失败一定由该限制触发。

修复后，文本请求与其他能力一样优先读取绑定的超时配置，支持 5–1800 秒，未配置或配置无效时仍使用原默认值（文本为 180 秒）。现有 GPT-6 流式请求方式及模型参数继续生效。

生效步骤：

1. 构建并部署包含本修复的 Web 服务；同步更新实际运行棒棒任务的实例。
2. 后台 → 逻辑模型 → 编辑项目选中的 `gpt-6` → 对应渠道绑定 → 能力档案 → “请求超时（秒）”，建议先设置为 **900** 并保存。存在多个可用绑定时，核对可能被选中的绑定各自的时限。
3. 在原“清洁剂”项目重新生成创作方向，确认结果保存。仅部署代码而未设置绑定时限，默认仍是 180 秒。

棒棒环节另有 25 分钟总时限，内部 HTTP 传输时限为 40 分钟；建议的 15 分钟模型请求时限低于两者。其他多次模型调用的步骤仍受环节总时限约束；上游网关仍可能更早终止请求。

新增回归使用模拟时钟和流式响应，验证绑定设为 15 分钟时，第 4 分钟返回的完整正文成功；未配置时仍在第 3 分钟超时；持续心跳不绕过配置的总时限；外层取消仍立即中止。未调用真实付费模型，未修改线上设置或部署。

## 2026-09-14：带货短剧使用独立的默认等待时限

用户再次提供创作方向“生成接口响应超时”的截图，并反馈等待超过 3 分钟。当前代码虽已支持绑定超时，但未配置绑定时仍使用 180 秒；图片下载和转换发生在模型计时开始前，因此页面等待可能略长于 3 分钟。随后通过 1Panel 核对正式环境：部署代码为 `4e160df`，该项目所用 `gpt-6` 绑定没有显式设置 `capabilityProfile.timeoutMs`，因此实际使用 180 秒默认值；正式日志中也存在该方向请求的 504 错误。

带货短剧的创作方向及后续文本／图片理解请求，现在未配置时默认等待 **900 秒**，优先遵守绑定中已设置的有效时限。策略仅应用于本次执行选出的候选，不修改已保存的渠道或绑定。其他业务的默认值、完整视频分析的既有时限、25 分钟环节总时限继续生效。没有增加自动重试。

共享视觉请求在自身等待时限触发时显示实际秒数，覆盖等待响应头和读取正文两个阶段；保留已收到的计费结算信息，供失败回滚退款。上游 HTTP 504 与调用方取消仍按各自原因报告。

验证使用模拟时钟及可取消的慢响应，实际执行候选解析、产品图片处理、流式读取与方向解析。隔离加载 HEAD 的 `bangbang-runtime.ts` 时，第 4 分钟返回完整方向的测试因 180 秒限制失败；当前实现通过同一测试。10 项定向回归覆盖默认等待、显式 3／20 分钟配置、心跳不延长总时限、25 分钟环节上限、无图片结构化请求、其他业务默认值、取消、上游 504 和按已结算积分退款。没有调用真实付费模型。

最终验证：4 个测试文件共 35 项通过；全项目 TypeScript、变更 TypeScript 文件的 ESLint 和 `git diff --check` 通过。

若绑定已显式设置为 180 秒，本修复会继续遵守该值，应在“逻辑模型 → 渠道绑定 → 能力档案 → 请求超时（秒）”调整为 900，或清空以采用带货短剧默认值。旧项目保存的错误不会自行消失，需要在原项目重新运行创作方向；产品图无需重传。

### 2026-09-14：上线前真实调用验证

通过已登录的 1Panel，在同一服务器构建提交 `6f5954f`，镜像为 `vozeb-pro:bangbang-check-6f5954f`，镜像 ID 为 `sha256:64c7edeabf101e4fbce12bd193c425c9406287f4a381ca5983ff3dd30a3f6776`。验证容器仅绑定 `127.0.0.1:3101`，使用独立数据库和独立媒体卷，沿用正式环境的模型配置及真实上游服务。

验证使用原“清洁剂”项目的产品图、产品信息、GPT-6 XHigh 模型和 120 秒目标时长，新建独立验证项目，实际调用 HTTP 接口完成以下流程：

1. 上传产品图片：241,731 字节，保存后的大小一致。
2. 生成创作方向：耗时 **494 秒（8 分 14 秒）**，保存 6,805 字符、3 个方向，推荐方向位于首位。
3. 选择推荐方向，再生成完整剧本：耗时 **368 秒（6 分 8 秒）**，保存 6,486 字符，包含四层结构、故事梗概与人物表。
4. 两步均通过新的 GET 请求重新读取，确认已保存文本与生成结果一致，无项目错误或仍在执行的操作。

服务端验证日志在 `2026-09-14T04:10:09.682Z` 报告 `verification-passed`。这次使用真实模型请求，两个生成步骤均超过原来的 180 秒限制；它证明该输入在本次上游响应时间内能够完整生成，不代表上游始终在 900 秒内完成。验证产物保存在服务器受保护目录 `/opt/vozeb-pro-bangbang-validation`。

已逐段检查生成正文：故事与推荐方向一致，包含人物表、8 个每段 15 秒的单元，以及覆盖到 120 秒的完整结尾。随后备份正式数据库、现有 Compose 文件和旧镜像，将正式源代码快进到 `6f5954f`，直接发布上述同一镜像到 Web 与 generation-worker，未重新构建另一个产物。服务器原有 Compose 修改和历史备份文件均保留。

部署后确认两个正式容器使用上述镜像 ID，Web 健康状态正常；内网和公网 `/api/health/ready` 均返回 `ready: true`，数据库和后台任务 Worker 均健康。旧镜像保留为 `vozeb-pro:before-bangbang-20260914`，本次备份与发布日志位于同一受保护验证目录。上线前确认没有正在执行的项目操作；8 条旧图像记录均处于 `needs_review`，没有任务租约，不是正在运行的模型请求。

最后在正式网站原“清洁剂”项目点击“根据产品生成剧本”，真实验证页面上的自动方向选择与剧本生成：

- 创作方向从 `04:22:03.687Z` 执行至 `04:29:04.614Z`，约 **7 分 1 秒**，保存 3 个方向、6,196 字符。
- 页面自动选中 A“搬走可以，这瓶先留下（推荐）”，剧本从 `04:29:09.594Z` 执行至 `04:33:25.778Z`，约 **4 分 16 秒**，保存 6,258 字符。
- 刷新页面后，方向及完整剧本仍在；通过页面导出文本包并读取其中的 `project.json`，确认项目版本为 37，错误与运行操作均为空。正文包含四层结构、故事梗概、人物表、连续覆盖 0–120 秒的 9 个单元及完整结尾，每个单元不超过 15 秒。
- 导出剧本 SHA-256 为 `6a817b4ce62f0ed546ece4319c7707f0de58b975da5aa8a86e94175ce090582d`；正式验证报告与完整项目副本已归档到服务器验证目录。独立验证容器已停止，保留验证产物与回滚备份。

本次验收覆盖产品图上传、创作方向、推荐方向自动选择、完整剧本、保存后刷新读取和文本导出；后续人物素材、分镜及图像生成不属于本次超时验收范围。
